[project]
name = "airflow"
version = "0.1.0"
description = ""
authors = [
    {name = "Felipe Vital", email = "felipezreal@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = ["apache-airflow (>=3.3.0,<4.0.0)"]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"